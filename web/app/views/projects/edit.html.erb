<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <%= link_to @project, class: "text-indigo-600 hover:text-indigo-800 inline-flex items-center" do %>
      <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      Voltar
    <% end %>
    <h1 class="text-4xl font-bold text-gray-900 mt-4">Editar Projeto</h1>
    <p class="text-gray-600 mt-2"><%= @project.name %></p>
  </div>

  <div class="bg-white rounded-lg shadow-sm p-6" data-controller="project-form" data-project-form-sheet-index-value="1000" data-project-form-piece-index-value="1000">
    <%= form_with model: @project, class: "space-y-6" do |f| %>
      <!-- Errors -->
      <% if @project.errors.any? %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4">
          <h3 class="text-sm font-medium text-red-800 mb-2">
            <%= pluralize(@project.errors.count, "erro") %> impediu que o projeto fosse salvo:
          </h3>
          <ul class="list-disc list-inside text-sm text-red-700">
            <% @project.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <!-- Informações Básicas -->
      <div class="border-b border-gray-200 pb-6">
        <h2 class="text-2xl font-semibold text-gray-900 mb-4">Informações Básicas</h2>
        
        <div class="space-y-4">
          <div>
            <%= f.label :name, "Nome do Projeto *", class: "block text-sm font-medium text-gray-700 mb-1" %>
            <%= f.text_field :name, required: true, class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent", placeholder: "Ex: Armário de Cozinha" %>
          </div>

          <div>
            <%= f.label :description, "Descrição", class: "block text-sm font-medium text-gray-700 mb-1" %>
            <%= f.text_area :description, rows: 3, class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent", placeholder: "Descrição opcional do projeto..." %>
          </div>

          <div class="space-y-4">
            <!-- Linha 1: Campos numéricos -->
            <div class="grid grid-cols-2 gap-4">
              <div>
                <%= f.label :cutting_width, "Espessura do Corte (mm)", class: "block text-sm font-medium text-gray-700 mb-1" %>
                <%= f.number_field :cutting_width, min: 1, step: 0.1, class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent" %>
                <p class="text-xs text-gray-500 mt-1">Espessura do corte da serra (padrão: 3mm)</p>
              </div>

              <div>
                <%= f.label :thickness, "Espessura da Chapa (mm)", class: "block text-sm font-medium text-gray-700 mb-1" %>
                <%= f.number_field :thickness, step: 0.1, min: 0.1, class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent", placeholder: "Ex: 15" %>
                <p class="text-xs text-gray-500 mt-1">
                  <strong>Obrigatório</strong> quando usar inventário. Filtra chapas com essa espessura.
                </p>
              </div>
            </div>

            <!-- Linha 2: Checkboxes -->
            <div class="grid grid-cols-3 gap-4">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">Permitir Rotação</label>
                <div class="flex items-center h-10">
                  <%= f.check_box :allow_rotation, class: "h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded" %>
                  <%= f.label :allow_rotation, "Sim, permitir rotação de peças", class: "ml-2 text-sm text-gray-700" %>
                </div>
              </div>

              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">🔪 Modo de Otimização</label>
                <div class="flex items-center h-10">
                  <%= f.check_box :guillotine_mode, class: "h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded" %>
                  <%= f.label :guillotine_mode, "Guilhotina", class: "ml-2 text-sm text-gray-700" %>
                </div>
              </div>

              <div>
                <label class="block text-sm font-medium text-gray-700 mb-2">📦 Origem das Chapas</label>
                <div class="flex items-center h-10">
                  <%= f.check_box :use_inventory, class: "h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded" %>
                  <%= f.label :use_inventory, "Usar inventário", class: "ml-2 text-sm text-gray-700" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Chapas -->
      <div>
        <div class="flex justify-between items-center mb-4">
          <h3 class="text-lg font-semibold text-gray-900">Chapas Disponíveis</h3>
          <button type="button" data-action="click->project-form#addSheet" class="bg-green-500 hover:bg-green-600 text-white font-medium py-2 px-4 rounded-lg transition duration-200">
            + Adicionar Chapa
          </button>
        </div>
        
        <div data-project-form-target="sheetsContainer" class="space-y-4">
          <%= f.fields_for :sheets do |sheet_fields| %>
            <%= render 'sheet_fields', f: sheet_fields %>
          <% end %>
        </div>
      </div>

      <!-- Peças -->
      <div>
        <div class="flex justify-between items-center mb-4">
          <h3 class="text-lg font-semibold text-gray-900">Peças Necessárias</h3>
          <button type="button" data-action="click->project-form#addPiece" class="bg-blue-500 hover:bg-blue-600 text-white font-medium py-2 px-4 rounded-lg transition duration-200">
            + Adicionar Peça
          </button>
        </div>
        
        <div data-project-form-target="piecesContainer" class="space-y-4">
          <%= f.fields_for :pieces do |piece_fields| %>
            <%= render 'piece_fields', f: piece_fields %>
          <% end %>
        </div>
      </div>

      <!-- Submit -->
      <div class="flex justify-end space-x-3 pt-6 border-t border-gray-200">
        <%= link_to "Cancelar", @project, class: "bg-gray-100 hover:bg-gray-200 text-gray-700 font-bold py-2 px-6 rounded-lg transition duration-200" %>
        <%= f.submit "Salvar Alterações", class: "bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-6 rounded-lg transition duration-200" %>
      </div>
    <% end %>
  </div>
</div>

<!-- Templates for nested forms (hidden) -->
<template data-project-form-target="sheetTemplate">
  <%= form_with model: @project, url: "" do |f| %>
    <%= f.fields_for :sheets, Sheet.new, child_index: 'NEW_SHEET_RECORD' do |sheet_fields| %>
      <%= render 'sheet_fields', f: sheet_fields %>
    <% end %>
  <% end %>
</template>

<template data-project-form-target="pieceTemplate">
  <%= form_with model: @project, url: "" do |f| %>
    <%= f.fields_for :pieces, Piece.new, child_index: 'NEW_PIECE_RECORD' do |piece_fields| %>
      <%= render 'piece_fields', f: piece_fields %>
    <% end %>
  <% end %>
</template>
